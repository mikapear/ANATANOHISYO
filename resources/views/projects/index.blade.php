<x-site-app title="暮らしの予定 | ANATANOHISHO"><div class="flex items-end justify-between gap-4"><div><h1 class="text-2xl font-bold text-indigo-950 sm:text-3xl">暮らしの予定</h1><p class="mt-2 text-sm text-stone-600">介護、仕事、家庭、旅行、趣味などの予定をまとめます。</p></div><a href="{{ route('projects.create') }}" class="primary-action">＋ 追加する</a></div>@php $labels=['active'=>'進行中','completed'=>'完了','on_hold'=>'保留']; $templateLabels=['planning'=>'計画','checkin'=>'習慣・服薬','journal'=>'活動・体調記録']; @endphp @if($projects->isEmpty())<div class="empty-panel"><p class="font-semibold text-indigo-900">暮らしの予定はまだありません。</p><p class="mt-2 text-sm text-stone-500">介護、仕事、家庭、旅行、趣味など、暮らしに合わせて作れます。</p></div>@else<div class="mt-8 grid gap-4 sm:grid-cols-2">@foreach($projects as $project)<a href="{{ route('projects.show',$project) }}" class="project-card"><div class="flex items-start justify-between gap-3"><h2 class="font-semibold text-indigo-950">{{ $project->name }}</h2><span class="project-status project-status--{{ $project->status }}">{{ $labels[$project->status] }}</span></div><p class="mt-2 text-xs font-medium text-stone-500">{{ $templateLabels[$project->template] ?? $project->template }}</p>@if($project->description)<p class="mt-3 line-clamp-2 text-sm leading-relaxed text-stone-600">{{ $project->description }}</p>@endif<div class="mt-4 flex flex-wrap gap-3 text-xs text-stone-500">@if($project->uses_todos)<span>やること {{ $project->todos_count }}件</span>@endif @if($project->uses_activity_logs)<span>記録 {{ $project->activity_logs_count }}件</span>@endif @if($project->uses_checkins)<span>習慣チェック</span>@endif @if($project->due_date)<span>期限 {{ $project->due_date->format('Y/n/j') }}</span>@endif</div></a>@endforeach</div>@endif</x-site-app>



