<x-site-app title="記録を編集 | ANATANOHISHO"><div class="mx-auto max-w-2xl"><h1 class="text-2xl font-bold text-indigo-950">記録を編集する</h1>@if($activityLog->photos->isNotEmpty())<div class="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3">@foreach($activityLog->photos as $photo)<div class="relative"><img src="{{ Storage::disk($photo->disk)->url($photo->path) }}" alt="{{ $photo->original_name }}" class="aspect-square w-full rounded-lg object-cover"><form method="POST" action="{{ route('activity-log-photos.destroy',$photo) }}" onsubmit="return confirm('この写真を削除しますか？')">@csrf @method('DELETE')<button class="absolute right-2 top-2 rounded bg-white/90 px-2 py-1 text-xs text-red-600">削除</button></form></div>@endforeach</div>@endif<form method="POST" enctype="multipart/form-data" action="{{ route('activity-logs.update',$activityLog) }}" class="form-panel">@method('PUT') @include('activity-logs._form',['submitLabel'=>'更新する','cancelUrl'=>route('activity-logs.show',$activityLog)])</form></div></x-site-app>
